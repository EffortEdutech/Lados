[
  {
    "type": "lados.human.request_approval",
    "displayName": "Request Approval",
    "canonicalCapability": "human.approval.request",
    "ownerPack": "lados.human-work",
    "status": "skeleton",
    "intent": "Pause or route work for a human approval decision.",
    "inputPattern": ["ports", "inspector", "resource_binding"],
    "outputPattern": "approval task reference and pending state",
    "ports": {
      "inputs": [{ "id": "context", "label": "Context", "dataType": "object" }],
      "outputs": [{ "id": "approvalTask", "label": "Task", "dataType": "object" }]
    },
    "configGroups": [
      { "id": "request", "label": "Request", "fields": ["title", "description", "assigneeRole"] },
      { "id": "deadline", "label": "Deadline", "fields": ["dueDate", "escalationRole"] }
    ],
    "resourceBindings": { "supported": true, "required": false },
    "knowledgePackRequirements": { "required": [], "recommended": ["approval matrix"] },
    "humanDecisionBoundary": "Requires a human actor to approve, reject, or request changes.",
    "aiBoundary": "none",
    "canvasUx": { "minWidth": 250, "minHeight": 120, "maxVisiblePortsPerSide": 2 },
    "compatibilityAliases": ["core.human_approval", "foundation.request_approval"],
    "executorStatus": "not_started"
  },
  {
    "type": "lados.human.record_decision",
    "displayName": "Record Decision",
    "canonicalCapability": "human.decision.record",
    "ownerPack": "lados.human-work",
    "status": "skeleton",
    "intent": "Record a human decision and its evidence without making the decision automatically.",
    "inputPattern": ["ports", "inspector", "resource_binding"],
    "outputPattern": "decision record",
    "ports": {
      "inputs": [{ "id": "evidence", "label": "Evidence", "dataType": "object" }],
      "outputs": [{ "id": "decision", "label": "Decision", "dataType": "object" }]
    },
    "configGroups": [
      { "id": "decision", "label": "Decision", "fields": ["decisionType", "decisionBy", "decisionDate"] },
      { "id": "record", "label": "Record", "fields": ["notes", "attachments"] }
    ],
    "resourceBindings": { "supported": true, "required": false },
    "knowledgePackRequirements": { "required": [], "recommended": [] },
    "humanDecisionBoundary": "The value must come from a human actor or existing approval record.",
    "aiBoundary": "none",
    "canvasUx": { "minWidth": 250, "minHeight": 120, "maxVisiblePortsPerSide": 2 },
    "compatibilityAliases": [],
    "executorStatus": "not_started"
  }
]
